<div class="config colorConfig {{extraClasses}}">
  <h1 class="propertyTite">{{title}}</h1>
  {{#each colors}}

    <button class="color"
            title="{{this.name}}"
            data-value="{{this.value}}"
            style="background-color: {{this.value}} !important"> </button>

  {{/each}}
</div>
